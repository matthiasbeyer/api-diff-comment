# Added API interfaces

{{#each added}}
* {{ this }}
{{/each}}

# Changed API interfaces

{{#each changed}}
* {{ this.old }} -> {{ this.new }}
{{/each}}

# Removed API interfaces

{{#each removed}}
* {{ this.old }} -> {{ this.new }}
{{/each}}
